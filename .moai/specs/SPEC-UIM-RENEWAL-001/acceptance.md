---
id: SPEC-UIM-RENEWAL-001
kind: acceptance
updated: 2026-07-26
---

# 인수 조건 — SPEC-UIM-RENEWAL-001

각 항목은 **기계로 확인 가능한 형태**로 적었다. 검증 명령이 실제로 실행되어 출력을 관찰한
경우에만 PASS로 표시한다.

## A. 빌드 · 타입

| AC | 조건 | 검증 |
|---|---|---|
| AC-B-01 | 타입 오류 0 | `npx tsc --noEmit` 종료 코드 0 |
| AC-B-02 | 프로덕션 빌드 성공 | `npm run build` 종료 코드 0 |
| AC-B-03 | 린트 통과 | `npm run lint` 종료 코드 0 |

## B. 라우팅 · 국제화

| AC | 조건 | 검증 |
|---|---|---|
| AC-R-01 | 3개 로케일 홈이 모두 생성된다 | 빌드 출력에 `/ja`, `/en`, `/ko` 존재 |
| AC-R-02 | 로케일 없는 경로는 감지 후 리다이렉트된다 | `/` 요청 시 302 + `Location: /{locale}` |
| AC-R-03 | 요금 페이지 세그먼트가 로케일별로 갈린다 | `/en/plans` 200, `/en/plan` 404, `/ja/plan` 200, `/ja/plans` 404 |
| AC-R-04 | 갤러리 필터가 경로다 | `/ja/gallery/studio/self-wedding` 200, canonical이 자기 경로 |
| AC-R-05 | 모든 페이지에 hreflang 3개 + x-default | 각 페이지 HTML에 `alternate` 링크 4개 |
| AC-R-06 | 404는 noindex | 404 응답 HTML에 `noindex` |

## C. 사업 규칙 (문자열 감사)

전부 `grep`으로 확인 가능한 형태로 정의했다. `src/` 아래에서 검사한다.

| AC | 조건 | 검증 |
|---|---|---|
| AC-BR-01 | 우천 대체 문구 없음 | `grep -riE '雨天|우천|if it rains' src/` 결과 0 |
| AC-BR-02 | 예약 기능 없음 | `grep -riE 'reserve\|예약 폼\|予約フォーム' src/app` 에 예약 라우트 없음 |
| AC-BR-03 | 인스타 임베드 없음 | `grep -rn 'instagram.com/p/\|instgrm' src/` 결과 0 |
| AC-BR-04 | 이메일 주소 하드코딩 없음 | `grep -rnE '[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}' src/` 에 실제 주소 0 |
| AC-BR-05 | 가격이 콘텐츠 모듈에만 존재 | `grep -rn '99,000\|76,000\|128,000' src/app src/components` 결과 0 |
| AC-BR-06 | EN 페이지에 일본식 문장부호 없음 | EN 콘텐츠에 `・` `／` `〜` 전각공백 0 |
| AC-BR-07 | 네이버 블로그 안내는 ko만 | `grep -rn -i 'naver' src/` 결과가 ko 분기 안에만 |

## D. 사진 파이프라인

| AC | 조건 | 검증 |
|---|---|---|
| AC-PH-01 | `PUBLISHED`만 프론트 노출 | 갤러리·홈 렌더 경로가 status 필터를 거친다 |
| AC-PH-02 | alt 3개 언어 없이 게시 불가 | API 핸들러가 3개 언어 alt 부재 시 400 |
| AC-PH-03 | 크론이 자격 증명 없이 성공을 보고하지 않음 | 환경 변수 미설정 시 503 + 사유 |
| AC-PH-04 | 크론이 인증 없이 호출되지 않음 | `CRON_SECRET` 불일치 시 401 |

## E. 문의

| AC | 조건 | 검증 |
|---|---|---|
| AC-IN-01 | 입력이 스키마로 검증된다 | 과길이·누락 필드 요청이 400 |
| AC-IN-02 | 응답이 제출 내용을 되돌려주지 않는다 | 성공 응답 본문에 입력 필드 없음 |
| AC-IN-03 | 저장이 알림보다 먼저다 | 핸들러 코드 순서 + 알림 실패 경로가 저장을 되돌리지 않음 |

## F. 관리자

| AC | 조건 | 검증 |
|---|---|---|
| AC-AD-01 | 관리자 API가 무인증으로 열리지 않는다 | 세션 없는 요청이 401 |
| AC-AD-02 | `/admin`, `/api`가 noindex | 응답 헤더 `X-Robots-Tag: noindex` |
| AC-AD-03 | 미연결 쓰기 경로가 거짓 성공을 내지 않는다 | 스텁이 값을 반환하지 않고 명시적으로 미구현을 알린다 |
| AC-AD-04 | 세션 없이 관리자 화면에 들어갈 수 없다 | `/admin/*` 요청이 307 → `/admin/login` |
| AC-AD-05 | 허용 목록 밖 구글 계정은 통과하지 못한다 | `signIn` 콜백이 목록 대조 후 거부, `?error=AccessDenied` 표시 |
| AC-AD-06 | 자격 증명 미설정이 통과로 해석되지 않는다 | 프로덕션에서 환경변수 누락 시 화면·API 모두 차단 |
| AC-AD-07 | 관리자 화면이 정적으로 굳지 않는다 | 빌드 출력에서 `/admin/*`가 전부 `ƒ`(동적) |
| AC-AD-08 | 미인증 요청에 관리자 페이지가 **렌더되지 않는다** | 미인증 응답 본문에 각 화면의 고유 문자열이 0회 — 레이아웃 가드는 자식 실행을 막지 못하므로 페이지 가드 + 미들웨어 2층으로 확인 |
| AC-AD-09 | 세션 쿠키를 위조해도 데이터가 나가지 않는다 | 위조 쿠키로 미들웨어를 통과시킨 뒤에도 프로브 0회 |
| AC-AD-10 | 유출 검사의 프로브가 살아 있음을 같은 방식으로 증명한다 | 가드가 열린 상태(dev + SSO 미설정)에서 같은 프로브가 1회 이상 — "0회"가 프로브 고장 때문이 아님을 보인다 |

## G. SEO / AEO

| AC | 조건 | 검증 |
|---|---|---|
| AC-SEO-01 | 모든 공개 페이지에 정의형 1문단 | 각 페이지 첫 텍스트 블록이 실제 텍스트 |
| AC-SEO-02 | sitemap이 갤러리 필터 조합을 포함 | `/sitemap.xml` 에 필터 URL 존재 |
| AC-SEO-03 | robots가 admin·api를 막고 sitemap을 가리킨다 | `/robots.txt` 내용 확인 |
| AC-SEO-04 | 구조화 데이터가 유효하다 | 각 JSON-LD가 파싱 가능하고 필수 필드 보유 |

## H. 접근성 · 성능

| AC | 조건 | 검증 |
|---|---|---|
| AC-A11Y-01 | 모바일 탭 타겟 44px 이상 | 모바일 뷰포트에서 링크·버튼 높이 측정 |
| AC-A11Y-02 | 본문 보조색 대비 4.5:1 이상 | `--muted-2` 이상으로 밝은 색 미사용 |
| AC-PERF-01 | 이미지에 width/height 명시 | `next/image` 사용, CLS 방지 |
| AC-PERF-02 | 히어로만 eager, 나머지 lazy | `priority` 사용 위치 확인 |

## 미검증 (Gaps)

이 SPEC의 범위 밖이라 **검증하지 않았음을 명시**한다.

- 실제 DB 연결 상태에서의 동작 — Prisma 클라이언트가 아직 설치되지 않았다
- Instagram Graph API 실호출 결과 — 자격 증명이 없다
- 오브젝트 스토리지 업로드 및 AVIF/WebP 인코딩 실행 — 미구현 지점(seam)
- Lighthouse 실측 점수 — 배포 후 측정 필요
- 브라우저 실렌더 픽셀 대조 — 시안과의 육안 대조는 수행하지 않았다
